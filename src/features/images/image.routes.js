const express = require('express');
const router = express.Router();

/**
 * Legacy image routes placeholder
 * New encrypted image routes are in: /api/images/encrypted/*
 */

// Legacy routes can be added here if needed
// For now, all image serving goes through encrypted-images.routes.js

module.exports = router;
